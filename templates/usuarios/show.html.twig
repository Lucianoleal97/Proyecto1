{% extends 'base.html.twig' %}

{% block title %}Usuarios{% endblock %}

{% block body %}
    <h1>Usuarios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Id_usuario_k</th>
                <td>{{ usuario.idUsuarioK }}</td>
            </tr>
            <tr>
                <th>Id_usuario_a</th>
                <td>{{ usuario.idUsuarioA }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('usuarios_index') }}">volver a la lista</a>

    <a href="{{ path('usuarios_edit', {'id': usuario.id}) }}">editar</a>

    {{ include('usuarios/_delete_form.html.twig') }}
{% endblock %}
